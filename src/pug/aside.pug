aside
  ul.social   
    each item in ["whatsapp", "viber", "telegram", "instagram"]
      li.social__item: a(href="#", title=`${item.charAt(0).toUpperCase() + item.slice(1)}`): img(inline src=require(`../img/${item}.svg`)) 
  div